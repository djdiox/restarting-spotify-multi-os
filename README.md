# Restart Spotify (Windows, Mac, Linux)

This package enables you to restart Spotify without issues, 
after running 
``npm i -g restart-spotify_multi-osv``.

Now you can run `restart-spotify` anywhere to restart spotify!